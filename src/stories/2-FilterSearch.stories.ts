import {FilterSearchComponent} from '../../projects/filter-search/src/public-api';

export default {
  title: 'Filter Search',
  component: FilterSearchComponent
};

export const FilterSearch = () => ({
  component: FilterSearchComponent,
});
